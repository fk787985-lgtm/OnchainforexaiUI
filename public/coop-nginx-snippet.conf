# Add this to the server block that serves https://onchainforexai.com
# (frontend). Without it, Chrome may block Google GIS popup postMessage (COOP).

add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;
# Do NOT set: Cross-Origin-Opener-Policy "same-origin";
